#include "Graph.h"

#include <vector>
#include <map>


void Dijkstra(IGraph* graph) {

}

int main(int argc, char** argv) {

    std::vector<std::map<int, float>> adj_map;
    VectorGraph graph(adj_map);
    
    return 0;
}